# ProNetworkAppSpot - Development Roadmap

# خارطة طريق التطوير

> **آخر تحديث**: 2026-02-18
> **الإصدار الحالي**: v1.1 - النظام الأساسي مكتمل

---

## المراحل العامة

| المرحلة | الوصف | الحالة |
|---------|-------|--------|
| المرحلة 1 | الشاشة الرئيسية Dashboard | ✅ مكتملة |
| المرحلة 2 | الألوان الذكية + الفرز المتقدم + دعم SETTLED | ✅ مكتملة |
| المرحلة 3 | إصدار الإنتاج v1.1 (APK) | ✅ مكتملة |
| المرحلة 4 | السيرفر المركزي + نظام المصادقة | ⭕ لم يبدأ |
| المرحلة 5 | نظام الصلاحيات والطلبات (Admin/User) | ⭕ لم يبدأ |
| المرحلة 6 | المزامنة (Offline-First + Sync) | ⭕ لم يبدأ |
| المرحلة 7 | دورة حياة العميل (Subscriber Lifecycle) | ⭕ لم يبدأ |
| المرحلة 8 | تحسين التصدير + QR Code | ⭕ لم يبدأ |
| المرحلة 9 | تقارير المقارنة + تحليل التسرب (Churn) | ⭕ لم يبدأ |
| المرحلة 10 | الإعدادات + الوضع الليلي + الثيمات | ⭕ لم يبدأ |
| المرحلة 11 | الإشعارات الذكية | ⭕ لم يبدأ |
| المرحلة 12 | الحماية (بصمة/رقم سري) | ⭕ لم يبدأ |

---

## فلسفة الترتيب

```
المرحلة 1-2-3: التطبيق الأساسي (مكتمل + إنتاج)
     ↓
المرحلة 4-5-6: البنية التحتية (سيرفر + صلاحيات + مزامنة)
     ↓
المرحلة 7-8-9: تحسينات متقدمة (تُبنى فوق البنية التحتية)
     ↓
المرحلة 10-11-12: تحسينات تجميلية وأمنية
```

**السبب**: نظام الصلاحيات والمزامنة يجب أن يُبنى قبل أي ميزة جديدة، لأن كل الميزات القادمة ستعتمد عليه. لو بنينا دورة حياة العميل أو التقارير المتقدمة قبل السيرفر، سنضطر لإعادة بنائها عند ربط السيرفر.

---

## المرحلة 1: الشاشة الرئيسية (Dashboard) ✅

**الأولوية**: عالية جداً | **الجهد**: متوسط | **التأثير**: عالي جداً

### الهدف
شاشة رئيسية تظهر عند فتح التطبيق تعطي لمحة شاملة عن حالة التحصيل والعملاء.

### ما تم تنفيذه
- ✅ إنشاء `DashboardScreen.kt`
- ✅ بطاقة ترحيب + 3 بطاقات KPI رئيسية
- ✅ بطاقة المقارنة (هذا الشهر vs الشهر السابق)
- ✅ قائمة "يحتاج انتباهك" (أعلى 5 عملاء غير مدفوعين)
- ✅ آخر العمليات (آخر 10 حركات تحصيل)
- ✅ أزرار سريعة (التحصيل اليومي + العملاء + الإحصائيات)
- ✅ دمج في `MainActivity.kt` كتبويب رئيسي
- ✅ بيانات Dashboard في `PaymentViewModel.kt`

---

## المرحلة 2: الألوان الذكية + الفرز المتقدم ✅

**الأولوية**: عالية | **الجهد**: منخفض | **التأثير**: عالي

### ما تم تنفيذه
- ✅ ألوان ذكية حسب حالة الدفع (🟢 PAID / 🟠 PARTIAL / 🔵 SETTLED / 🔴 UNPAID)
- ✅ نظام الفرز المتقدم (`ViewOptionsDialog` + `SortOption`)
- ✅ فرز حسب: الاسم / الحالة / السعر / المبنى / الباقة / تاريخ البداية
- ✅ دعم كامل لحالة SETTLED (كشف الاسترجاع التلقائي)
- ✅ فلاتر حسب المبنى والباقة

---

## المرحلة 3: إصدار الإنتاج v1.1 (APK) ✅

**الأولوية**: عالية جداً | **الجهد**: منخفض | **التأثير**: عالي جداً

### الهدف
إصدار نسخة إنتاج مستقرة وتوزيعها على العاملين للاستخدام الفوري. التطبيق يعمل محلياً (Offline) بالكامل.

### الخطوات
- ✅ Build > Generate Signed APK
- ✅ إنشاء Keystore وحفظه بأمان
- ✅ توزيع APK على العاملين

> **ملاحظة**: هذا الإصدار يعمل بدون سيرفر. كل عامل عنده قاعدة بيانات مستقلة على جواله. عند إصدار v2.0 (بعد المرحلة 6) سيتم توحيد البيانات عبر السيرفر.

---

## المرحلة 4: السيرفر المركزي + نظام المصادقة

**الأولوية**: عالية جداً | **الجهد**: عالي | **التأثير**: جذري

### الهدف
بناء سيرفر API مركزي على Proxmox يكون الأساس لكل شي بعده (صلاحيات، مزامنة، طلبات).

### البنية

```
┌──────────────────────────────────────────────┐
│            سيرفرك (Proxmox)                   │
│                                              │
│   ┌──────────────────────────────┐           │
│   │   Docker Container           │           │
│   │   ┌────────────────────┐     │           │
│   │   │  FastAPI Server      │     │           │
│   │   │  (Python)            │     │           │
│   │   └────────────────────┘     │           │
│   │   ┌────────────────────┐     │           │
│   │   │  PostgreSQL          │     │           │
│   │   └────────────────────┘     │           │
│   └──────────────────────────────┘           │
│                                              │
└──────────────────────────────────────────────┘
        ↑           ↑           ↑
    📱 أنت       📱 عامل 1    📱 عامل 2
    (Admin)      (User)       (User)
```

### 4.1 إعداد Docker على Proxmox
- [ ] إنشاء CT/VM جديد للسيرفر
- [ ] تثبيت Docker + Docker Compose
- [ ] إعداد `docker-compose.yml` (FastAPI + PostgreSQL)
- [ ] ربط السيرفر بشبكة Tailscale (للوصول من الخارج)

### 4.2 بناء FastAPI Server
- [ ] إنشاء مشروع FastAPI أساسي
- [ ] إعداد قاعدة بيانات PostgreSQL + SQLAlchemy
- [ ] جداول: `users`, `sessions`, `sync_log`

### 4.3 نظام المصادقة (Authentication)
- [ ] API: `POST /auth/login` (تسجيل دخول → JWT Token)
- [ ] API: `POST /auth/logout`
- [ ] API: `GET /auth/me` (معلومات المستخدم الحالي)
- [ ] نظام JWT Token مع Refresh Token
- [ ] تشفير كلمات المرور (bcrypt)

### 4.4 إدارة المستخدمين (Admin فقط)
- [ ] API: `POST /admin/users` (إنشاء مستخدم جديد)
- [ ] API: `GET /admin/users` (قائمة المستخدمين)
- [ ] API: `PUT /admin/users/{id}` (تعديل مستخدم)
- [ ] API: `DELETE /admin/users/{id}` (حذف مستخدم)
- [ ] API: `PUT /admin/users/{id}/toggle` (تفعيل/تعطيل)

### جدول users

```sql
CREATE TABLE users (
    id SERIAL PRIMARY KEY,
    username VARCHAR(50) UNIQUE NOT NULL,
    password_hash VARCHAR(255) NOT NULL,
    display_name VARCHAR(100) NOT NULL,
    role VARCHAR(20) NOT NULL DEFAULT 'USER',  -- ADMIN / USER
    is_active BOOLEAN DEFAULT true,
    created_at TIMESTAMP DEFAULT NOW(),
    last_login TIMESTAMP
);
```

---

## المرحلة 5: نظام الصلاحيات والطلبات (Admin/User)

**الأولوية**: عالية جداً | **الجهد**: عالي | **التأثير**: جذري

### الهدف
بعض العمليات يقدر العامل يسويها مباشرة، وبعضها تحتاج موافقة المدير.

### جدول الصلاحيات

| العملية | Admin (أنت) | User (العامل) |
|--------|------------|-------------|
| عرض العملاء والمباني | مباشر | مباشر |
| تسجيل دفعة / تحصيل | مباشر | مباشر |
| إضافة عميل جديد | مباشر | مباشر |
| حذف عميل/مبنى | مباشر | 🟡 يحتاج طلب موافقة |
| سحب تقرير/تصدير | مباشر | 🟡 يحتاج طلب موافقة |
| فصل خدمة عميل | مباشر | 🟡 يحتاج طلب موافقة |
| إدارة المستخدمين | مباشر | 🔴 ممنوع |
| الموافقة/الرفض على الطلبات | مباشر | 🔴 ممنوع |

### 5.1 نظام الطلبات (Approval Requests)

```
العامل يضغط "حذف عميل"
    → يظهر له "تم إرسال الطلب للمدير"
    → عندك في التطبيق 🔔 إشعار
    → تضغط ✅ موافق أو ❌ رفض
    → العامل يشوف النتيجة
```

### 5.2 APIs المطلوبة
- [ ] `POST /requests` (إنشاء طلب جديد)
- [ ] `GET /requests` (قائمة الطلبات - للمدير)
- [ ] `GET /requests/my` (طلباتي - للعامل)
- [ ] `PUT /requests/{id}/approve` (موافقة)
- [ ] `PUT /requests/{id}/reject` (رفض)

### 5.3 جدول approval_requests

```sql
CREATE TABLE approval_requests (
    id SERIAL PRIMARY KEY,
    requester_id INT REFERENCES users(id),
    request_type VARCHAR(50) NOT NULL,    -- DELETE_CLIENT / DELETE_BUILDING / EXPORT_REPORT / DISCONNECT_CLIENT
    target_id INT,                         -- ID العنصر المطلوب
    target_name VARCHAR(200),              -- اسم العنصر (للعرض)
    reason TEXT,                            -- سبب الطلب (اختياري)
    status VARCHAR(20) DEFAULT 'PENDING',  -- PENDING / APPROVED / REJECTED
    reviewed_by INT REFERENCES users(id),
    created_at TIMESTAMP DEFAULT NOW(),
    reviewed_at TIMESTAMP
);
```

### 5.4 تعديل التطبيق (Android)
- [ ] إضافة شاشة تسجيل الدخول (`LoginScreen.kt`)
- [ ] حفظ JWT Token في `EncryptedSharedPreferences`
- [ ] إضافة شاشة إدارة المستخدمين (`UserManagementScreen.kt` - Admin فقط)
- [ ] إضافة شاشة الطلبات (`ApprovalRequestsScreen.kt`)
- [ ] تعديل أزرار الحذف/التصدير/الفصل لترسل طلب بدل التنفيذ المباشر (للـ User)
- [ ] إضافة tab إدارة في NavigationBar (للـ Admin)

---

## المرحلة 6: المزامنة (Offline-First + Sync)

**الأولوية**: عالية جداً | **الجهد**: عالي جداً | **التأثير**: جذري

### الهدف
التطبيق يشتغل Offline بالكامل (قاعدة Room المحلية). لما يرجع Online يتزامن تلقائياً مع السيرفر.

### آلية العمل

```
التطبيق Online:
  كل عملية → تُحفظ في Room محلياً + تُرسل للسيرفر فوراً
  السيرفر يرسل تحديثات الأجهزة الأخرى

التطبيق Offline:
  كل عملية → تُحفظ في Room محلياً + تُسجل في طابور المزامنة (sync_queue)
  لما يرجع Online → يرسل كل الطابور للسيرفر
```

### 6.1 تعديل Room Database
- [ ] إضافة جدول `sync_queue` محلي (العمليات المعلقة)
- [ ] إضافة حقل `last_synced` لكل سجل
- [ ] إضافة حقل `server_id` لربط السجل المحلي بالسيرفر
- [ ] Migration للحقول الجديدة

### 6.2 إنشاء SyncManager.kt
- [ ] كشف حالة الاتصال (ConnectivityManager)
- [ ] إرسال العمليات المعلقة عند عودة الاتصال
- [ ] جلب التحديثات من السيرفر
- [ ] حل التعارضات (Conflict Resolution: server wins)

### 6.3 APIs المزامنة
- [ ] `POST /sync/push` (إرسال العمليات المحلية للسيرفر)
- [ ] `GET /sync/pull?since={timestamp}` (جلب التحديثات من السيرفر)
- [ ] `GET /sync/status` (حالة المزامنة)

### 6.4 مؤشر حالة الاتصال في التطبيق
- [ ] شريط علوي: 🟢 Online / 🔴 Offline / 🟡 جاري المزامنة...
- [ ] عداد العمليات المعلقة

> **مهم**: بعد إكمال هذه المرحلة يتم إصدار v2.0 وتوزيعه على العاملين. التحديث يضيف شاشة تسجيل الدخول فقط — باقي الشاشات ما يتغير عليهم شي.

---

## المرحلة 7: دورة حياة العميل (Subscription Lifecycle)
**الأولوية**: متوسطة | **الجهد**: متوسط | **التأثير**: عالي

### الهدف
تتبع اشتراكات العملاء (تاريخ البداية، الانتهاء، التجديد) مع تنبيهات تلقائية قبل انتهاء الاشتراك.

### 7.1 تعديل قاعدة البيانات
- [ ] إضافة حقول الاشتراك (`subscription_start`, `subscription_end`, `subscription_plan`)
- [ ] إضافة جدول `subscription_history` لتتبع التجديدات
- [ ] Migration للحقول الجديدة

### 7.2 شاشة إدارة الاشتراكات
- [ ] عرض حالة الاشتراك لكل عميل (نشط / منتهي / قارب على الانتهاء)
- [ ] فلترة العملاء حسب حالة الاشتراك
- [ ] تجديد الاشتراك يدوياً
- [ ] تعديل خطة الاشتراك

### 7.3 التنبيهات التلقائية
- [ ] تنبيه قبل انتهاء الاشتراك بـ 7 أيام
- [ ] تنبيه عند انتهاء الاشتراك
- [ ] تنبيه للعملاء المتأخرين عن التجديد
- [ ] `WorkManager` لجدولة فحص الاشتراكات يومياً

### 7.4 APIs الاشتراكات (السيرفر)
- [ ] `PUT /subscribers/{id}/subscription` (تجديد/تعديل الاشتراك)
- [ ] `GET /subscribers/expiring?days={n}` (العملاء القاربين على الانتهاء)
- [ ] `GET /subscribers/{id}/subscription-history` (سجل الاشتراكات)

---

## المرحلة 8: تحسين التصدير + QR Code
**الأولوية**: متوسطة | **الجهد**: متوسط | **التأثير**: عالي

### الهدف
تحسين آلية تصدير بيانات المشتركين وإضافة دعم QR Code للوصول السريع لبيانات العميل.

### 8.1 تحسين التصدير
- [ ] تصدير Excel (بدل CSV فقط) باستخدام Apache POI
- [ ] اختيار الأعمدة المراد تصديرها
- [ ] تصدير PDF مع تنسيق جدولي
- [ ] مشاركة الملف مباشرة عبر Intent
- [ ] حفظ الملف في مجلد مخصص (`ProNetwork/Exports/`)

### 8.2 QR Code للمشتركين
- [ ] توليد QR Code لكل مشترك (يحتوي على الـ ID + الاسم)
- [ ] ماسح QR Code للبحث السريع عن مشترك
- [ ] طباعة بطاقة المشترك مع QR Code
- [ ] مكتبة `zxing-android-embedded` للتوليد والقراءة

---

## المرحلة 9: تقارير المقارنة + تحليل التسرب (Churn Analysis)
**الأولوية**: متوسطة | **الجهد**: عالي | **التأثير**: عالي

### الهدف
تقارير تحليلية لمقارنة الأداء بين الفترات وتحليل أسباب فقدان العملاء.

### 9.1 تقارير المقارنة
- [ ] مقارنة شهر بشهر (عدد المشتركين / الإيرادات / الانقطاعات)
- [ ] رسوم بيانية للترندات (MPAndroidChart)
- [ ] اختيار فترة المقارنة (مخصص)
- [ ] تصدير التقارير كـ PDF

### 9.2 تحليل التسرب (Churn)
- [ ] تتبع المشتركين اللي ما جددوا اشتراكهم
- [ ] أسباب الإلغاء (حقل اختياري عند الفصل)
- [ ] نسبة التسرب الشهرية (Churn Rate)
- [ ] قائمة العملاء المعرضين للتسرب (At Risk)
- [ ] اقتراحات لتقليل التسرب

### 9.3 لوحة التحليلات (Analytics Dashboard)
- [ ] مؤشرات الأداء الرئيسية (KPIs)
- [ ] متوسط الإيراد لكل عميل (ARPU)
- [ ] معدل الاحتفاظ بالعملاء (Retention Rate)
- [ ] إجمالي الإيرادات الشهرية (MRR)

---

## المرحلة 10: الإعدادات + الوضع الليلي + الثيمات
**الأولوية**: منخفضة | **الجهد**: متوسط | **التأثير**: متوسط

### الهدف
إعدادات شاملة للتطبيق مع دعم الوضع الليلي وتخصيص المظهر.

### 10.1 شاشة الإعدادات (`SettingsScreen.kt`)
- [ ] إعدادات المظهر (فاتح / داكن / تلقائي حسب النظام)
- [ ] إعدادات اللغة (عربي / إنجليزي)
- [ ] إعدادات التنبيهات (تفعيل/تعطيل)
- [ ] إعدادات المزامنة (تلقائي / يدوي)
- [ ] معلومات التطبيق (الإصدار + السيرفر)
- [ ] حفظ الإعدادات في `DataStore` (بديل SharedPreferences)

### 10.2 الوضع الليلي (Dark Mode)
- [ ] دعم Material 3 Dark Theme
- [ ] تبديل سلس بين الأوضاع
- [ ] حفظ اختيار المستخدم
- [ ] تطبيق الثيم على جميع الشاشات

### 10.3 الثيمات المخصصة
- [ ] اختيار لون التطبيق الرئيسي (Dynamic Color)
- [ ] دعم Material You (الألوان من خلفية النظام - Android 12+)
- [ ] ثيمات مسبقة الإعداد (أزرق / أخضر / بنفسجي)

---

## المرحلة 11: الإشعارات الذكية (Smart Notifications)
**الأولوية**: منخفضة | **الجهد**: عالي | **التأثير**: عالي

### الهدف
نظام إشعارات ذكي ينبه المدير عن الأحداث المهمة تلقائياً.

### 11.1 أنواع الإشعارات
- [ ] اشتراك قارب على الانتهاء (قبل 7 أيام / 3 أيام / يوم)
- [ ] اشتراك منتهي
- [ ] مشترك جديد تم إضافته
- [ ] طلب موافقة جديد (من نظام الصلاحيات)
- [ ] فشل في المزامنة
- [ ] إشعارات السيرفر (Push Notifications عبر FCM)

### 11.2 إعدادات الإشعارات
- [ ] تفعيل/تعطيل كل نوع على حدة
- [ ] تحديد وقت الإشعارات (عدم الإزعاج ليلاً)
- [ ] Notification Channels منفصلة لكل نوع

### 11.3 التنفيذ التقني
- [ ] `WorkManager` للإشعارات المحلية
- [ ] Firebase Cloud Messaging (FCM) للإشعارات من السيرفر
- [ ] صفحة سجل الإشعارات (`NotificationHistoryScreen.kt`)
- [ ] باج عدد الإشعارات غير المقروءة

---

## المرحلة 12: الحماية (بصمة / رقم سري) (App Security)
**الأولوية**: منخفضة | **الجهد**: متوسط | **التأثير**: عالي

### الهدف
حماية التطبيق بطبقة أمان إضافية (بصمة / رقم سري) لمنع الوصول غير المصرح به.

### 12.1 قفل التطبيق
- [ ] قفل بالبصمة (BiometricPrompt API)
- [ ] قفل برقم سري (PIN - 4/6 أرقام)
- [ ] قفل بنمط (Pattern Lock)
- [ ] اختيار طريقة القفل من الإعدادات

### 12.2 سياسات الأمان
- [ ] قفل تلقائي عند الخروج من التطبيق
- [ ] قفل بعد فترة خمول (1/5/15 دقيقة)
- [ ] مسح البيانات بعد 10 محاولات فاشلة (اختياري)
- [ ] تسجيل محاولات الدخول الفاشلة

### 12.3 التنفيذ التقني
- [ ] `BiometricPrompt` API (AndroidX Biometric)
- [ ] `EncryptedSharedPreferences` لحفظ الـ PIN
- [ ] شاشة القفل (`LockScreen.kt`)
- [ ] شاشة إعداد القفل (`SecuritySettingsScreen.kt`)

---

## ملاحظات عامة

### ترتيب التنفيذ
```
المرحلة 1-3: مكتملة (v1.1)
المرحلة 4: السيرفر المركزي ← الأساس لكل شي
المرحلة 5: نظام الصلاحيات ← يعتمد على السيرفر
المرحلة 6: المزامنة ← يعتمد على السيرفر + الصلاحيات
المرحلة 7-12: تحسينات مستقلة (يمكن تنفيذها بأي ترتيب)
```

### الإصدارات المخططة
| الإصدار | المحتوى | الحالة |
|-------|---------|-------|
| v1.1 | الشاشة الرئيسية + الألوان + APK | ✅ مكتمل |
| v2.0 | السيرفر + الصلاحيات + المزامنة + تسجيل دخول | 🟡 قيد التخطيط |
| v3.0 | الاشتراكات + التقارير + QR Code | 🔴 لم يبدأ |
| v4.0 | الإشعارات + الإعدادات + الحماية | 🔴 لم يبدأ |

### التقنيات المستخدمة
- **التطبيق**: Kotlin + Jetpack Compose + Room + Hilt
- **السيرفر**: Node.js/Express + PostgreSQL + JWT
- **البنية التحتية**: Docker + Proxmox
- **المزامنة**: REST API + Offline-First Architecture
- **الإشعارات**: FCM + WorkManager
