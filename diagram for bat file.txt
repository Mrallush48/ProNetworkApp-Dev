┌─────────────────────────────┐
│      📝 حفظ التعديلات       │
│ save_local.bat              │
│ → git add + git commit      │
│ (محلي فقط، بدون رفع)       │
└───────────────┬─────────────┘
                │
                ▼
┌─────────────────────────────┐
│ 🚀 رفع التعديلات إلى Dev   │
│ push_dev.bat                │
│ → git add + commit + push   │
│ إلى dev/master              │
└───────────────┬─────────────┘
                │
                ▼
┌─────────────────────────────┐
│ 🚀 رفع التعديلات إلى Origin│
│ push_origin.bat             │
│ → git add + commit + push   │
│ إلى origin/master           │
└───────────────┬─────────────┘
                │
                ▼
┌─────────────────────────────┐
│ 🔄 سحب آخر نسخة من Dev     │
│ pull_dev.bat                │
│ → git fetch + reset --hard  │
│ (يحذف أي تعديلات غير محفوظة)│
└───────────────┬─────────────┘
                │
                ▼
┌─────────────────────────────┐
│ 🔄 استرجاع من الريبو الأساسي│
│ reset_origin.bat            │
│ → git fetch + reset --hard  │
│ (يحذف أي تعديلات غير محفوظة)│
└─────────────────────────────┘
